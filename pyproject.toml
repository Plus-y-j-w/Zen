[project]
name = "zen"
version = "0.1.0"
description = "Zen FastAPI Cloudflare Workers Application"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "python-jose",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]

[tool.uvicorn]
factory = false
